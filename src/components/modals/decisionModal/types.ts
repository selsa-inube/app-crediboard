export interface IDecisionEntry {
  justification: string;
}

export enum EComponentAppearance {
  DANGER = "danger",
  DARK = "dark",
  GRAY = "gray",
  HELP = "help",
  LIGHT = "light",
  PRIMARY = "primary",
  SUCCESS = "success",
  WARNING = "warning",
}
